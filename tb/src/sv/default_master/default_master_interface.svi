`ifndef DEFAULT_MASTER_INTERFACE_SVI
`define DEFAULT_MASTER_INTERFACE_SVI

interface default_master_interface();

  clocking cb @(posedge hclk);
  endclocking

  clocking mon_cb @(posedge hclk);
  endclocking


  modport DUT (
  );

  modport MON (
  );

endinterface

`endif
