`ifndef DEFAULT_SLAVE_INTERFACE_SVI
`define DEFAULT_SLAVE_INTERFACE_SVI

interface default_slave_interface();

  clocking cb @(posedge hclk);
  endclocking

  clocking mon_cb @(posedge hclk);
  endclocking


  modport DUT (
  );

  modport MON (
  );

endinterface

`endif
